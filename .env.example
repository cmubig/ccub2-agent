# Local environment configuration (copy to .env and customize)
# This file shows available environment variables for local development

# Data directory (where images, indices, etc. are stored)
# Default: ~/ccub2-agent-data
# Example for custom location:
# DATA_DIR=/scratch/chans/ccub2-agent-data
